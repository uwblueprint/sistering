import { Text } from "@chakra-ui/react";
import React from "react";

const NoShiftsAvailableTableRow = (): React.ReactElement => {
  return <Text color="text.gray">No shifts available</Text>;
};

export default NoShiftsAvailableTableRow;
